"""
Unified AI job search and application automation platform.

One service layer behind four front ends (REST API, CLI, MCP server, web
dashboard), so business logic lives in exactly one place.
"""

__version__ = "0.1.0"
